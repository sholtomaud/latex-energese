% The same diagram as source-store.tex, authored directly in TeX instead of
% JSON. Both go through the same layout engine, so the output is identical.
\documentclass{standalone}
\usepackage{energese}
\begin{document}
\begin{energese}[background rectangle/.style={fill={rgb,255:red,237;green,237;blue,234}}, show background rectangle]
    \esmeta{column_spacing=3.0, row_spacing=1.5, show_heat_sink=false}

    \esnode{src}{source} {Tr=1,   x=1.0, y=3.6, magnitude=0.9}
    \esnode{st}{storage} {Tr=100, x=3.8, y=3.6, magnitude=1.2,
                          label={Q}, label_options={inner sep=1pt, font=\sffamily\large}}
    \esnode{sink}{text}  {x=6.8, y=1.8}
    \esnode{gnd}{ground} {x=5.4, y=0.5}
    \esnode{branch}{text}{x=5.4, y=2.5}
    \esnode{eq_box}{box} {x=3.4, y=0.8, magnitude=1.3, label={$\dot{Q} = J_0 - kQ$}}

    \esflow{src}{st}{energy}       {volume=0.5, label={$J_0$},
                                    label_options={midway, above, inner sep=5pt}}
    \esflow{st}{branch}{energy}    {volume=0.5, label={$kQ$},
                                    label_options={midway, above, inner sep=3pt},
                                    options={bend left=20, -}}
    \esflow{branch}{sink}{energy}  {volume=0.5, options={->}}
    \esflow{branch}{gnd}{energy}   {volume=0.5, options={-}}
\end{energese}
\end{document}
